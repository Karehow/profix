# ตรรกะระบบยืม–คืนสิ่งของวัด

## วงจรคำขอยืม

`pending_approval` → `approved` → `borrowed` → `return_requested` → `returned` หรือ `partially_damaged`

- ผู้ยืมส่งคำขอแล้ว เจ้าหน้าที่ตรวจจำนวนและอนุมัติหรือปฏิเสธ
- เมื่ออนุมัติ ระบบกันของออกจากยอดพร้อมยืมทันที
- เจ้าหน้าที่ต้องกดยืนยันส่งมอบ จึงถือว่าของอยู่กับผู้ยืม
- ผู้ยืมกดแจ้งพร้อมคืน แล้วเจ้าหน้าที่จึงตรวจรับจำนวนและสภาพจริง
- คำขอที่ยังไม่อนุมัติหรือยังไม่ส่งมอบยกเลิกได้; ถ้ากันของแล้วระบบคืนยอดให้อัตโนมัติ

## ของแบบชุด

- ผู้ยืมยืมชุดมาตรฐานได้หลายชุดต่อคำขอ และส่งคำขอของชุดเดิมเพิ่มได้เมื่อของย่อยยังคงเหลือ
- เลือกชุดมาตรฐาน หรือระบุจำนวนรวมของของย่อยแต่ละชนิด เช่น พานทอง 3 ใบ พานเงิน 1 ใบ
- รายการของย่อยถูกบันทึกเป็น snapshot ณ เวลาส่งคำขอ การแก้ชื่อ/จำนวนของชุดภายหลังจึงไม่เปลี่ยนประวัติเดิม
- หน้ารายการสิ่งของแสดง “ของไม่ครบชุด” เมื่อของย่อยที่เหลือประกอบเป็นชุดไม่ครบ หรือมีชนิดที่เจ้าหน้าที่ระบุว่าไม่พร้อมให้ยืม แต่ยังยืมของย่อยที่เหลือได้ แม้ชุดมาตรฐานคงเหลือ 0
- เจ้าหน้าที่ระบุ “ของย่อยรายการนี้หมด / ไม่พร้อมให้ยืม” ได้ในฟอร์มเพิ่มหรือแก้ไขของย่อย เมื่อนำของกลับมาเติม ให้เอาเครื่องหมายออก สถานะนี้ใช้กับของย่อยรายการนั้นทุกชุด โดยไม่เปลี่ยนจำนวนมาตรฐานต่อชุดและประวัติเดิม
- สต็อกตัดตามจำนวนของย่อยจริง ชุดมาตรฐานที่พร้อมยืมคำนวณจากจำนวนของย่อยที่ประกอบครบได้ ระบบตรวจยอดจองและสต็อกซ้ำตอนอนุมัติ
- อัปเกรดสต็อกแยกของย่อยด้วย `php database/migrate_component_stock.php` หรือ migration หลัก ดูรายละเอียดใน `database/COMPONENT_STOCK.md`
- ชุดมาตรฐานคืนบางชุดได้ รายการที่กำหนดจำนวนของย่อยเองต้องตรวจรับรายการนั้นพร้อมกันทั้งหมดและระบุปัญหาแยกตามชนิด
- ตอนคืน เจ้าหน้าที่แจ้งของย่อยที่ชำรุดหรือสูญหายหลายรายการพร้อมกันได้ แต่แจ้ง “ตัวชุดหลัก” พร้อมของย่อยไม่ได้ เพื่อป้องกันการนับซ้ำ

## กฎสต็อก

- `total_quantity` คือจำนวนทรัพย์สินที่วัดยังถือครอง
- `available_quantity` คือจำนวนที่พร้อมให้ยืม และต้องอยู่ระหว่าง 0 ถึงจำนวนทั้งหมดเสมอ
- อนุมัติยืม/ส่งซ่อม: ลดยอดพร้อมยืม
- คืนสภาพดี/ซ่อมเสร็จ/รับของทดแทนครบ: เพิ่มยอดพร้อมยืม
- รับชำระกรณีของสูญหายหรือใช้ต่อไม่ได้: ตัดจำนวนดังกล่าวออกจากยอดทั้งหมด
- ทุกการเปลี่ยนยอดบันทึกใน `inventory_movements` เพื่อสอบย้อนหลังได้

## ความเสียหายและการชดใช้

- ของสภาพดีกลับเข้าคลังทันที ส่วนจำนวนที่มีปัญหาถูกพักไว้
- แต่ละปัญหากำหนดได้ว่าจะนำของมาทดแทนหรือชำระเงิน และบันทึกการชดใช้ได้ครั้งเดียว
- คำขอปิดยอดชดใช้เมื่อปัญหาทุกรายการได้รับการจัดการแล้ว
- สำหรับชุด ของย่อยที่คืนสภาพดีพร้อมให้ยืมต่อทันที การชดใช้คืนหรือตัดสต็อกเฉพาะชนิดที่มีปัญหา โดยไม่ตัดของย่อยอื่นออกจากคลัง

## การจองล่วงหน้า

- ระบบตรวจช่วงเวลาซ้อนกับการจองที่อนุมัติ ของที่กำลังยืม และของระหว่างซ่อม
- ของแบบชุดจองได้ครั้งละ 1 ชุด และช่วงจองหนึ่งรายการไม่เกิน 31 วัน
- เมื่ออนุมัติแล้ว ผู้ยืมสร้างคำขอยืมได้ภายใน 24 ชั่วโมงก่อนเริ่มช่วงจอง
- คำขอยืมปกติเลือกรับล่วงหน้าได้ไม่เกิน 7 วัน; ระยะยืมไม่เกิน 31 วัน

## สิทธิ์และความปลอดภัย

- ลืม PIN: ผู้ยืมติดต่อเจ้าหน้าที่เพื่อยืนยันตัวตน เจ้าหน้าที่ใช้เมนู “ผู้ยืมลืม PIN” ค้นหาเบอร์โทรและออกรหัสรีเซ็ตให้เจ้าของบัญชีที่ตรวจสอบแล้ว ผู้ยืมกรอกรหัสในเมนู “ลืม PIN?” ที่หน้าเข้าสู่ระบบเพื่อตั้ง PIN ใหม่
- รหัสรีเซ็ตใช้ได้ครั้งเดียวภายใน 15 นาที เก็บเฉพาะแฮชในฐานข้อมูล การออกรหัสใหม่ยกเลิกรหัสเดิม และการตั้ง PIN ใหม่ยกเลิกเซสชันผู้ยืมเดิมทั้งหมด บันทึกการออกและใช้รหัสใน audit log โดยไม่บันทึกรหัสหรือ PIN
- ระบบนี้ใช้เจ้าหน้าที่ตรวจสอบตัวตน ยังไม่มีบริการส่ง SMS/OTP หากติดตั้งระบบเดิมอยู่แล้ว เพิ่มคอลัมน์เฉพาะฟีเจอร์นี้ด้วย `php database/migrate_pin_reset.php` หรือใช้ migration หลักตามปกติ

- ผู้ยืมเข้าสู่ระบบด้วยเบอร์โทรศัพท์และ PIN 6 หลัก
- เจ้าหน้าที่/ผู้ดูแลเข้าสู่ระบบด้วยชื่อผู้ใช้และรหัสผ่านที่เข้ารหัสด้วย `password_hash`
- ทุกหน้าตรวจบทบาทจากฐานข้อมูล, การเปลี่ยนข้อมูลใช้ CSRF token และการเข้าสู่ระบบมีการจำกัดจำนวนครั้งที่ลองผิด
- บัญชีและสิ่งของใช้การปิดใช้งานแทนการลบ เพื่อรักษาประวัติ
- การเปลี่ยนสถานะสำคัญบันทึกทั้ง `borrow_request_events` และ `audit_logs`

## หลังนำโค้ดรุ่นนี้ไปใช้

สำรองฐานข้อมูลก่อน แล้วรัน migration จากโฟลเดอร์โครงการ:

```powershell
C:\xampp\php\php.exe database\migrate.php
```

Migration ออกแบบให้รันซ้ำได้ และไม่ลบรายการยืมหรือข้อมูลเดิม
